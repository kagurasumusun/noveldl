# 小説データベースの構造(調査・評価)

対象:`novel_core` の保存層(`src/nc_storage.cpp`)。要望「適切な構造になっているか?分けとか、階層?別のデータベース?調べて」への回答。

## 現在の構造(3層)

```
NovelLibrary/                          … アプリの書庫ルート
├── master.db                          … 目録(カタログ層)
│   ├── novels       (作品:ID/題名/著者/サイト/話数/更新時刻)
│   └── zstd_dictionaries / images     … 共有辞書・画像表
└── novels/
    └── <domain>/                      … サイト(ドメイン)単位の階層
        └── <novel_id>/                … 作品単位のシャード群
            ├── 0000.db … 000N.db      … 章の本文(章番号ハッシュで分割)
            │   ├── sections           (本文/前書き/後書きをzstd圧縮、署名、取得状態)
            │   └── sections_fts       (FTS5 全文検索)
```

| 層 | 中身 | 持ち方 |
|---|---|---|
| カタログ | 作品一覧・メタ | `master.db` 1つ + インデックス |
| 作品 | 章の本文 | **作品ごとに別の SQLite ファイル群**(章レンジでシャード) |
| 検索 | 本文全文検索 | 各シャード内の FTS5 仮想テーブル |

## 評価:この構造は適切か

**結論:方針は業界の推奨パターンと一致しており、適切。** 根拠:

1. **データベース分割(= sharding / database-per-tenant)** — SQLite 公式の適用方針でも、書き込み同時性を避ける有効な手段として「サブドメイン/ユーザーごとに別ファイル」が明示的に挙げられている([SQLite: Appropriate Uses](https://www.sqlite.org/whentouse.html))。本構造は「サイト → 作品」の単位で分離しており同じ思想。近年の SQLite 実装知見でも、`tenant_id` を全テーブルに持つ単一 DB より **DB-per-tenant(=作品単位分離)が分離・削除・移動の面で有利**と整理されている([The SQLite Renaissance, 2026](https://dev.to/pockit_tools/the-sqlite-renaissance-why-the-worlds-most-deployed-database-is-taking-over-production-in-2026-3jcc))。
2. **階層(サイト → 作品 → 章シャード)** — ファイルシステムが階層を担い、SQL は各階層内で単純に保てる。1作品の削除・書き出し(ZIP)・持ち出しが「フォルダ単位の操作」で完結する(実装もその通り)。
3. **章シャード(`NNNN.db`)** — 1作品が数千話(900〜数千)でも、1ファイルに全章を押し込まず章番号で分散。1画面ぶんの読書 = 1ファイルの読取に収まり、起動・読書が重くならない。
4. **FTS5 はシャード内** — 全文検索は「検索したい作品のシャード」だけで完結。大規模 FTS をシャード分割して運用する実運用報告([FTS5 + sharding の実例](https://news.ycombinator.com/item?id=41207085))と同じで、巨大単一インデックスを避ける正しい選択。
5. **本文の zstd 圧縮** — 端末容量と I/O を節約。ただし iOS では zstd は同梱せず未圧縮/差し替え可能(Nyxian ビルドでは zstd 無効で運用可)。

「単一の SQLite に全部」は、数千話を抱えたときの VACUUM・書き込みロック・バックアップ粒度の悪化で不利。「Core Data」はモデル変更の移行コストが高く、生 HTML/XHTML を圧縮 BLOB で持つ用途には噛み合わない。「1話=1ファイル」はファイル数が数千になり一覧性・検索性で不利。よって現状の**カタログ1本 + 作品ごとシャード + 内蔵FTS**が、このアプリの要件(端末ローカル・低書き込み同時性・作品単位の削除/書出・全文検索)には最適に近い。

## 改善メモ(必要になったら)

| 項目 | 内容 |
|---|---|
| WAL モード | 各シャードで `PRAGMA journal_mode=WAL`(読書中の書き込み衝突を緩和) |
| 整合性 | 起動時 `PRAGMA quick_check` をシャード単位で(破損シャードの隔離) |
| 古い章の整理 | 既読読済みの圧縮辞書(`zstd_dictionaries`)の世代管理 |
| マイグレーション | `PRAGMA user_version` によるスキーマ版管理(現在は IF NOT EXISTS で追加的更新) |
